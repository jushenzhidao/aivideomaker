# aivideomaker → 火山方舟 Seedance 协议兼容层（src/ark_compat/）
#
#   pip install -r requirements.txt
#   export AVM_KEY="ak_xxx"
#   python3 src/ark_server.py --port 8808
#
# 注：src/avm.py（官方 API CLI）与 src/web_session.py 仍为零依赖的标准库实现，
# 不依赖本文件中的任何包。

# HTTP 服务
fastapi>=0.115
# 🔴 **必须显式声明**：`request.form()`（OpenAI `/v1/videos` 收 multipart 表单用，见
#    `app.py` 的 `_openai_fields`）在 starlette 里是**可选**能力 —— fastapi/starlette
#    都不把本包列为依赖，缺它时**也不是导入期报错**，而是**第一个 multipart 请求**才抛
#    `AssertionError: The python-multipart library must be installed to use form parsing`
#    （starlette/requests.py 的 _get_form）。
#    2026-09-15 实测踩到：本地 venv 恰好装着（0.0.32）⇒ 550 项全绿，而 CI 干净环境
#    6 项失败、**镜像同样缺**（Dockerfile 只装本文件）。属"本地恰好装着、干净环境才现形"那一类。
python-multipart>=0.0.20
uvicorn>=0.30
httpx>=0.27

# 生产服务器：gunicorn + uvicorn worker（配置见 src/gunicorn_conf.py）
# ⚠️ 不要用 uvicorn 自带的 uvicorn.workers —— 该模块已废弃（导入即告警，指向
#    uvicorn-worker）。worker_class 走 uvicorn_worker.UvicornWorker。
gunicorn>=22
uvicorn-worker>=0.3

# 日志与可观测性
loguru>=0.7
# ⚠️ 必须带 `[fastapi]` extra —— 少了它，`logfire.instrument_fastapi(app)` 会抛
#    "requires the opentelemetry-instrumentation-fastapi package"，`app.py` 把异常吞成一条
#    警告后继续起服务 ⇒ **每个请求的自动 span 静默消失**（容器日志里每人 2 行，看不出后果）。
#    2026-09-14 报告 AVM12-OPEN-LOGFIRE 实测发现：`logfire>=3.0` 不带 extra 时，
#    `requirements.txt` 里搜 `logfire[fastapi]` 是 0 命中，而追踪链路上少了一层。
logfire[fastapi]>=3.0

# 测试（TestClient 依赖 httpx，已在上面）
