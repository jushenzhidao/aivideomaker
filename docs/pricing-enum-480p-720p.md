# 480p / 720p × 1–10 秒 计费枚举（web 内部接口）

> 采集时间：2026-09-13 · 适用模型：MiniMax H3（web 端模型名 `default`）
> 取数口径见文末「证据与局限」，**本表是文档口径的枚举，不是实测账单**。

> ## 🔴 2026-09-14 更正：本表有两处已被后续实测推翻 / 更新（表体保持原样以存史）
>
> 1. **免费窗口是 10s，不是 8s。** 文中「免费的是 ≤8 秒」「计费区（>8s）」「3 积分/秒」
>    里的分界都要顺延一档：`tier=turbo` 且 `duration ≤ 10s` **免费**，`≥ 11s` 计费。
>    依据是站点终态任务记录 `480p/10s/turbo` ⇒ `taskStatus=succeed` + **`paid=False`**。
> 2. **「480p 最长 8 秒 / 9–10s 站点未提供 ⊘」这条推断不成立。** 它来自页面文案
>    （"480p 免费生成，最长 8 秒"），但**同一份实测证据（480p/10s 成功且免费）已经证伪它**
>    —— 480p 的合法时长同样是 **5–20 连续秒数**（与 720p/1080p 一致，见
>    `translate.DURATION_ALLOWED`）。故主表 480p 列的 `9s` / `10s` 两行应视为
>    **已被证伪**，不要再据此判断"480p 做不了 9s+"。
> 3. 由 1、2 推论：本表把 480p 写成"只有 5s 免费档（+未实测的 6–8s）"也已过时 ——
>    480p 的免费档同样覆盖 **5–10s 任意整数秒**。
>
> 追溯记录见 `.workbuddy/memory/` 当日日志的「用户更正：免费窗口是 ≤10s」一节。

> ## 🔴 2026-09-24 更正（二次）：480p 时长是**离散档位**
>
> 上一节第 2 条的「480p 合法时长同样是 **5–20 连续秒数**」**已被真实提交证伪**：
> 站点对 480p 只收离散的 `5 / 10 / 15 / 20`，站点原话（真实提交被拒时返回）：
> `480p supports 5s, 10s, 15s, or 20s duration.`（报告 `E2E-AVM-016` + 生产 38 次 502 实锤）。
> 适配层（`translate.DURATION_ALLOWED`）已于 2026-09-24 **改回离散表 + 就近吸附并留痕**：
> 6/7→5、8/9→10、11/12→10、13/14→15、16/17→15、18/19→20。
> ⇒ 主表 480p 列按"**仅 5 与 10 两档免费**"读；720p / 1080p 不受影响（1080p 离散与否未测）。
>

## 0. 先说一条前置事实

**1–4 秒不存在。** H3 的**输入**合法区间是 **5–20 秒整数**（越界就近钳制）；但
**480p 只收离散档位 `5 / 10 / 15 / 20`**（2026-09-24 已实施就近吸附，见顶部二次更正横幅）——
只有 720p / 1080p 是连续区间。
（依据：`videoDurationMaxWarnTip` 描述连续上限、720p 侧 5/6/7/8/9/10/11/14s 逐秒实测全成功；
480p 的离散性由真实提交实测 `480p supports 5s, 10s, 15s, or 20s duration.` 确立，两者不矛盾。）

适配层对越界值做**就近吸附**：`1–4s → 5s`。所以下表的 1–4s 行不是"另一个价位"，
而是"会被静默改成 5s"。

## 1. 主表（模型 `default`）

单位：积分。免费窗口以 `paid=false` 为准（**不要看 `credits` 字段，它与 `paid` 反相**）。

| 时长 | 480p（快速） | 720p 快速（turbo） | 720p 质量（base） |
|---|---|---|---|
| 1s | 不存在 → 吸附 5s | 不存在 → 吸附 5s | 不存在 → 吸附 5s |
| 2s | 不存在 → 吸附 5s | 不存在 → 吸附 5s | 不存在 → 吸附 5s |
| 3s | 不存在 → 吸附 5s | 不存在 → 吸附 5s | 不存在 → 吸附 5s |
| 4s | 不存在 → 吸附 5s | 不存在 → 吸附 5s | 不存在 → 吸附 5s |
| 5s | **0**（免费草稿，已实测） | **0**（订阅用户免费，已实测） | 20 |
| 6s | **0** ⚠️ | **0**（已实测） | 24 |
| 7s | **0** ⚠️ | **0**（已实测） | 28 |
| 8s | **0** ⚠️ | **0**（已实测） | 32 |
| 9s | 站点未提供 ⊘ | 27 | 36 |
| 10s | 站点未提供 ⊘ | 30 | 40 |

- **480p 9–10s ⊘**：文案限定"480p 免费生成，**最长 8 秒**"，超出应不被站点接受。
- **⚠️ 480p 6–8s**：文案称免费至 8 秒，但实测样本（`model.listModel`）里 480p 只出现过 5s，
  6/7/8s 的 480p 免费**未逐条实测**。对外宣传口径是"不限量生成 **5 秒** 480p 草稿"。
- **720p 质量（base）**：文案「质量模式每秒增加 1 积分」，且 `base` 档**没有免费窗口**
  （"免费生成暂不支持质量模式"）⇒ 5s 起全程计费。
- 网页定价页另称订阅用户"无限 720p"，与上表 9–10s 计费并不矛盾 —— 免费的是 **≤8 秒**。
- 计费区（>8s）的每秒单价为 **3 积分/秒**（720p turbo）。

## 2. 证据与局限

| 项 | 来源 | 等级 |
|---|---|---|
| web 720p 3 积分/秒、480p 最长 8s、质量 +1 | `docs/web-reverse/captured/docs-zh.html` i18n（`model_default_description`、`minimaxTierQualityCredits`） | 页面文案（公开可复采） |
| web ≤8s 免费 | 实测任务记录 `model.listModel`，`paid=false` | **实测** |
| 合法时长 5–20 连续 | 站点文案 + 720p 逐秒实测 | **实测** |
| web 480p 6–8s 免费 | 无样本 | ⚠️ **未实测** |
| web 480p 9–10s | 文案限定最长 8 秒 | 推断，未实测 |

**未做**：同参数 A/B 实测。障碍是站点任务记录的 `credits` 与 `paid` 反相，
读不出单次扣分数 —— 若要实测，只能比对 `credits.getCredits` 的 `totalRemaining` 余额差。
